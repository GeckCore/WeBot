plugins/download.js has been modified, but it's a source code file (.js) that shouldn't be ignored. There are no build artifacts, dependencies, or temporary files in the changes that would require adding ignore rules to .gitignore.

(no output)